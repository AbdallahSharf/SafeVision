# =============================================================================
# SafeVision — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your real values:
#   cp .env.example .env
#
# NEVER commit .env to version control.
# =============================================================================

# ── Required ─────────────────────────────────────────────────────────────────

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?appName=MyApp

# RTSP camera URL (use your DDNS hostname for remote access)
RTSP_URL=rtsp://admin:password@your-ddns-hostname.com:554/cam/realmonitor?channel=1&subtype=0

# ── Server ───────────────────────────────────────────────────────────────────

# Port for the FastAPI server (default: 8080)
# PORT=8080

# Set to "true" to enable local OpenCV window display (disable on servers)
# DISPLAY_OUTPUT=false

# ── Model Paths ──────────────────────────────────────────────────────────────

# YOLO_MODEL_PATH=models/best.pt
# ARCFACE_MODEL_PATH=models/w600k_r50.onnx

# ── Detection Thresholds ─────────────────────────────────────────────────────

# YOLO confidence threshold (0.0–1.0)
# YOLO_CONF_THRESHOLD=0.4

# Bounding box confidence threshold
# BOX_CONF_THRESHOLD=0.6

# Face recognition similarity threshold
# RECOG_THRESHOLD=0.6

# ── Frame Processing ─────────────────────────────────────────────────────────

# FRAME_WIDTH=800
# FRAME_HEIGHT=600
# FACE_SIZE=112
# FACE_MARGIN=20
# IMGSZ=640
# QUEUE_SIZE=5
# HISTORY_LEN=5
# QUEUE_TIMEOUT=2.0

# ── MongoDB Vector Search ────────────────────────────────────────────────────

# DB_NUM_CANDIDATES=100
# DB_TOP_K=5

# ── Low-Light Enhancement ────────────────────────────────────────────────────

# Enable/disable low-light auto-enhancement
# LOW_LIGHT_ENABLE=true

# Mean brightness threshold (0–255); enhancement triggers below this
# LOW_LIGHT_AUTO_THRESHOLD=80

# CLAHE clip limit (2.0–4.0; higher = stronger contrast boost)
# CLAHE_CLIP_LIMIT=3.0

# CLAHE grid tile size
# CLAHE_TILE_SIZE=8

# Denoise strength (0=off, 3–10=light–strong; higher costs FPS)
# DENOISE_STRENGTH=7
