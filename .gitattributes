# Models are stored in Google Cloud Storage, NOT fetched via Git LFS.
# The workflow downloads them with gsutil at Docker build time.
# LFS tracking is intentionally disabled to avoid LFS bandwidth quota issues.
